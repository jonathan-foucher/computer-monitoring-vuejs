{
  "name": "computer-monitoring-server",
  "version": "1.0.0",
  "description": "The server side of the project, collects data from the computer and sends them to the client side.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathan Foucher",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
